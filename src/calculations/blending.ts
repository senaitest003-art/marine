import {Fuel,Settings,Year} from '../types';import {blendCI,effectiveCI,reward,targetCI} from './fuelEU';import {energyToMass} from './units';
export interface BlendResult{feasible:boolean;energyShare:number;massShare:number;actualCI:number;altTonnes:number;baseTonnes:number;reason?:string}
export const minimumBlend=(base:Fuel,alt:Fuel,year:Year,s:Settings,energyGJ:number,slip?:number):BlendResult=>{const t=targetCI(year,s),b=effectiveCI(base,year,s),a=effectiveCI(alt,year,s,slip),r=reward(alt,year,s);if(!alt.eligible||alt.maxSupply<=0||a>=t||a>=b)return{feasible:false,energyShare:NaN,massShare:NaN,actualCI:b,altTonnes:0,baseTonnes:energyToMass(energyGJ,base.lcv),reason:'Not Feasible'};const x=(b-t)/(b-t+r*(t-a));if(!Number.isFinite(x)||x>alt.maxBlend||x>1)return{feasible:false,energyShare:x,massShare:NaN,actualCI:b,altTonnes:0,baseTonnes:0,reason:'Not Feasible'};const share=Math.max(x,alt.minBlend),at=energyToMass(energyGJ*share,alt.lcv),bt=energyToMass(energyGJ*(1-share),base.lcv);if(at>alt.maxSupply)return{feasible:false,energyShare:share,massShare:NaN,actualCI:b,altTonnes:at,baseTonnes:bt,reason:'Supply constrained'};return{feasible:true,energyShare:share,massShare:at/(at+bt),actualCI:blendCI(base,alt,share,year,s,slip),altTonnes:at,baseTonnes:bt};};
